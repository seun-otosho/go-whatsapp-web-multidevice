# Environment Variables for Railway Deployment
# Copy these to your Railway project Variables tab

# =============================================================================
# ESSENTIAL VARIABLES (Required)
# =============================================================================

# Application port (Railway sets this automatically, but we override)
APP_PORT=3000

# Device name shown in WhatsApp (customize if needed)
APP_OS=Railway-WhatsApp

# Enable debug logging (set to false in production)
APP_DEBUG=false

# Database connection (SQLite - no setup required)
DB_URI=file:storages/whatsapp.db?_foreign_keys=on

# Disable account validation for faster setup
WHATSAPP_ACCOUNT_VALIDATION=false

# =============================================================================
# OPTIONAL VARIABLES (Recommended for Production)
# =============================================================================

# Basic Authentication (username:password format)
# Replace 'admin:yourStrongPassword' with your credentials
# Example: APP_BASIC_AUTH=admin:mypassword123
APP_BASIC_AUTH=admin:changeThisPassword

# Auto-mark incoming messages as read
WHATSAPP_AUTO_MARK_READ=true

# Webhook URL for incoming messages (optional)
# WHATSAPP_WEBHOOK=https://your-webhook-site.com/handler

# Webhook secret for validation (change from default 'secret')
WHATSAPP_WEBHOOK_SECRET=changeThisWebhookSecret

# Base path for subpath deployment (use "/" for root)
APP_BASE_PATH=/

# =============================================================================
# DATABASE OPTIONS (Choose One)
# =============================================================================

# Option 1: SQLite (Default - Recommended)
# No additional config needed
DB_URI=file:storages/whatsapp.db?_foreign_keys=on

# Option 2: PostgreSQL (If you added PostgreSQL addon to Railway)
# Uncomment the line below and comment out the SQLite line above
# DB_URI=${DATABASE_URL}

# =============================================================================
# ADVANCED VARIABLES (Optional)
# =============================================================================

# Auto-reply message (optional)
# WHATSAPP_AUTO_REPLY="Thanks for your message! We'll get back to you soon."

# Enable chat storage (recommended)
WHATSAPP_CHAT_STORAGE=true

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Go to Railway Dashboard → Your Project → Variables
# 2. Click "New Variable" for each of the above
# 3. Copy the variable name and value exactly as shown
# 4. Save all variables
# 5. Deploy the app
#
# For APP_BASIC_AUTH, WHATSAPP_WEBHOOK_SECRET: Change the default values!
#
# =============================================================================
